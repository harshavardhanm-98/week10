#include <stdio.h>
int main() {
    int arr[] = {10, 20, 30};
    int *ptr = arr; 
     printf("Address at ptr: %p, Value: %d\n", (void*)ptr, *ptr);
    ptr++; 
    printf("Address after ptr++: %p, Value: %d\n", (void*)ptr, *ptr);
    return 0;
}
